[Unit]
Description=Herdr OMX status reconcile watcher
After=default.target

[Service]
Type=simple
ExecStart=/bin/sh -lc 'exec "$HOME/.local/bin/herdr-omx-reconcile-watch"'
Restart=always
RestartSec=3
Environment=HERDR_OMX_WATCH_INTERVAL=1

[Install]
WantedBy=default.target
